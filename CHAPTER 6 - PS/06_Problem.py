# if the names of 2 friends are same, what will happen to the program in problem 5/
# The last entry will overwrite the previous one, as dictionaries do not allow duplicate keys.
# If you want to keep all entries, you could use a list as the value for each key.
